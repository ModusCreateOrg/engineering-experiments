/**
 * Find Voter by primary key.
 * @param {string} voterId A voter identifier.
 * @returns A `Voter` object or null if not found.
 */
exports.find = async (voterId) => {
  console.log('VoterService::find');
};
