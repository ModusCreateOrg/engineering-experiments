handler: src/index.onDisconnect
events:
  - websocket:
      route: $disconnect

package:
  patterns:
    - src/websocket/onDisconnect.handle

iamRoleStatements:
  - Effect: Allow
    Action:
      - dynamodb:DeleteItem
    Resource:
      - Fn::GetAtt: [ConnectionsWebsocketTable, Arn]      