handler: src/index.onConnect
events:
  - websocket:
      route: $connect

package:
  patterns:
    - src/websocket/onConnect.handle

iamRoleStatements:
  - Effect: Allow
    Action:
      - dynamodb:PutItem
    Resource:
      - Fn::GetAtt: [ConnectionsWebsocketTable, Arn]