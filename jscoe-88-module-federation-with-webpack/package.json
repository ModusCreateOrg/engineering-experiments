{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna clean --yes && npx lerna bootstrap",
    "build:watch": "npx lerna run build:watch --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}