{
  "name": "base-app",
  "version": "1.0.0",
  "description": "This is the main container / base application which consumes microfronteds",
  "scripts": {
    "start": "node ./runwebpack.config.js --mode=development --port=8080",
    "build": "node ./runwebpack.config.js --mode=production",
    "serve": "npx serve dist -p 8080",
    "build-serve": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.0",
    "serve": "13.0.2",
    "runwebpack": "^0.0.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}