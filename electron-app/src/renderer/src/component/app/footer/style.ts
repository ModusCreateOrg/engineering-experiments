import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  footer: {
    marginBottom: '0.5rem',
    bottom: 0,
    position: 'fixed',
    justifyContent: 'end',
    display: 'flex',
    width: '100%',
  },
}));
